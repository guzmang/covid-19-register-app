{
    "name": "covid-19",
    "version": "1.0.0",
    "description": "Challenge MELI",
    "main": "index.js",
    "scripts": {
        "start": "node server/server.js",
        "test": "NODE_ENV=server/tests nyc --exclude=tests --exclude=exception --reporter=lcov --reporter=text ./node_modules/mocha/bin/mocha server/tests/**/*spec.js --reporter spec",
        "coverage": "./node_modules/.bin/nyc report --statements 80 --functions 80 --lines 80 --branches 80 --check-coverage --reporter=text-summary --per-file"
    },
    "author": "German Guzman",
    "license": "ISC",
    "dependencies": {
        "body-parser": "^1.19.0",
        "express": "^4.17.1",
        "mongoose": "^5.9.20"
    },
    "devDependencies": {
        "chai": "^4.2.0",
        "mocha": "^8.0.1",
        "mockgoose": "^8.0.4",
        "nyc": "^15.1.0",
        "supertest": "^4.0.2"
    }
}
